## Summary

<!-- What changed and why (1–3 bullets). -->

## Test plan

- [ ] `bash Scripts/smoke.sh` passes
- [ ] GitHub Actions **Smoke (structure)** is green
- [ ] GitHub Actions **Build & test (macOS)** is green (when available)
- [ ] On a Mac (optional for docs-only): open vault → edit → autosave → preview → wiki link

## Notes

CI is defined in `.github/workflows/ci.yml`. No GitHub Release or Package is required for CI to run.
